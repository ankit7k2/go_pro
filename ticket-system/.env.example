# Secret key used to sign and verify JWTs.
# In production, set this to a long, random value and keep it secret.
JWT_SECRET=change_me_to_a_long_random_string

# Port the server listens on. Defaults to 8080 if unset.
PORT=8080
